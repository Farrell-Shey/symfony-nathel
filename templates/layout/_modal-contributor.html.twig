{% block modal_contributor %}

	{% embed "/component/_svg-global.html.twig" %}
		{% block body %}
			<div class="modal global-modal-contributor" id="staticBackdrop-contributor" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
				<div class="modal-dialog">
					<div class=" mx-auto modal-content modal-add-contributor">
						<div class="d-flex flex-end justify-content-end">
							<button type="button" class="close-btn" data-bs-dismiss="modal" aria-label="Close">Close</button>
						</div>

						<div>
							<div class="d-flex block-search-user">
								<div class="d-flex ">
									{%  if poolset_data is defined %}
										<input class="search-user bg-transparent" type="text" placeholder="Search user" data-id="{{ poolset_data['pool_id'] }}">
									{% else %}
										<input class="search-user bg-transparent" type="text" placeholder="Search user">
									{% endif %}

								</div>
							</div>
							<div class="contributor-container">
								<div class="d-flex search-result-block scrollbar">

								</div>
							</div>

							<span class="text-add">You want to add :</span>
							<div class="d-flex block-user-selected invisible-scrollbar">
							</div>
							<div class="d-flex flex-end justify-content-end">
								<button type="button" class="add-btnERROR" data-bs-dismiss="modal"  aria-label="Confirm"  data-id="{{ poolset_data['pool_id'] }}">Confirm</button>
							</div>
						</div>

					</div>
				</div>
			</div>
		{% endblock %}
	{% endembed %}
{% endblock %}
