{% extends 'base.html.twig' %}

{% block title %}PoolSet{% endblock %}

{% block body %}
    <h1>PoolSet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pool_set.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pool_set.name }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ pool_set.createdAt ? pool_set.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ pool_set.updatedAt ? pool_set.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Thumbnail</th>
                <td>{{ pool_set.thumbnail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pool_set_index') }}">back to list</a>

    <a href="{{ path('pool_set_edit', {'id': pool_set.id}) }}">edit</a>

    {{ include('pool_set/_delete_form.html.twig') }}
{% endblock %}
