<form method="post" action="{{ path('mappool_delete', {'id': mappool.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mappool.id) }}">
    <button class="btn">Delete</button>
</form>
